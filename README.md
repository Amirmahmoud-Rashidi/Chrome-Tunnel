# Chrome-Tunnel
This repo provides a tool for apps that use http/https request(Get, Post, etc) and support proxy to tunnel APP's traffic to chrome and use a VPN to bypass the 403 error instead of using VPN on OS that most of times it does not work
